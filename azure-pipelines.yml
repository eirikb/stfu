trigger:
- master

pool:
  vmImage: 'ubuntu-latest'

steps:
- task: NodeTool@0
  inputs:
    versionSpec: '10.x'
  displayName: 'Install Node.js'

- script: |
    npm install
    npm run build
  displayName: 'npm install and build'


- task: AzureFileCopy@3
  inputs:
    SourcePath: 'dist'
    azureSubscription: 'Visual Studio Enterprise – MPN(145fb177-89a7-4412-9c1f-6802580c3624)'
    Destination: 'AzureBlob'
    storage: 'stfueirikb'
    ContainerName: '$web'